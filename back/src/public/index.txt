<!--index.php-->
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ERP Ligero</title>
    <style>
        body { font-family: Arial, sans-serif; text-align: center; padding: 20px; }
        #app { max-width: 400px; margin: auto; }
        input, button { display: block; width: 100%; margin: 10px 0; padding: 10px; }
        .hidden { display: none; }
    </style>
</head>
<body>
    <div id="app">
        <h2>ERP - Login</h2>
        <div id="login-form">
            <input type="email" id="email" placeholder="Correo electrónico">
            <input type="password" id="password" placeholder="Contraseña">
            <button onclick="login()">Iniciar sesión</button>
            <button onclick="goToRegister()">Registrarse</button>
        </div>
        
        <div id="dashboard" class="hidden">
            <h2>Bienvenido</h2>
            <button onclick="logout()">Cerrar sesión</button>
            <button onclick="goToTask()">Gestión de Tareas</button>
            <button onclick="goToestadoCuenta()">Estado de Cuenta</button>
            <button onclick="goToclientes()">Alta de clientes</button>
        </div>

        <!--<div id="dashboard" class="hidden">
            <h2>Bienvenido</h2>
            <button onclick="logout()">Cerrar sesión</button>
            <button onclick="goToTask()">Gestión de Tareas</button>
            <button onclick="goToestadoCuenta()">Estado de Cuenta</button>
        </div>-->
    </div>

    <script>
        async function login() {
            const email = document.getElementById('email').value;
            const password = document.getElementById('password').value;

            const response = await fetch('/api/auth/login', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ email, password })
            });

            const data = await response.json();

            if (response.ok) {
                localStorage.setItem('accessToken', data.accessToken);
                document.getElementById('login-form').classList.add('hidden');
                document.getElementById('dashboard').classList.remove('hidden');
            } else {
                alert('Error: ' + data.error);
            }
        }

        function goToRegister() {
            window.location.href = "register.html";
        }

        function goToTask() {
            window.location.href = "task.html";
        }

        function goToestadoCuenta() {
            window.location.href = "estadoCuenta.html";
        }

        function goToclientes() {
            window.location.href = "clientes.html";
        }

        function logout() {
            localStorage.removeItem('accessToken');
            document.getElementById('login-form').classList.remove('hidden');
            document.getElementById('dashboard').classList.add('hidden');
        }
    </script>
</body>
</html>